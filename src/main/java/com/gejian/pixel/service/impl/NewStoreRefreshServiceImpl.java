package com.gejian.pixel.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.gejian.pixel.entity.NewStoreRefresh;
import com.gejian.pixel.mapper.NewStoreRefreshMapper;
import com.gejian.pixel.service.NewStoreRefreshService;
import org.springframework.stereotype.Service;

/**
 * Auto created by codeAppend plugin
 */
@Service
public class NewStoreRefreshServiceImpl extends ServiceImpl<NewStoreRefreshMapper, NewStoreRefresh> implements NewStoreRefreshService {

    // use "baseMapper" to call jdbc
    // example: baseMapper.insert(entity);
    // example: baseMapper.selectByPage(params);
   
}