package com.gejian.pixel.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.gejian.pixel.entity.NewStore;
import com.gejian.pixel.mapper.NewStoreMapper;
import com.gejian.pixel.service.NewStoreService;
import org.springframework.stereotype.Service;

/**
 * Auto created by codeAppend plugin
 */
@Service
public class NewStoreServiceImpl extends ServiceImpl<NewStoreMapper, NewStore> implements NewStoreService {

    // use "baseMapper" to call jdbc
    // example: baseMapper.insert(entity);
    // example: baseMapper.selectByPage(params);
   
}